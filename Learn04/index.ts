function add(num1: number, num2: number) {
  return num1 + num2;
}
console.log(add(10, 10)); // Output: 15

function etc(a: number, b: number, c: string): string {
  return a + b + c;
}
console.log(etc(10, 10, "hello")); // Output: 20hello
